# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'componentT' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
   pod 'TRUNetworking2', :path => 'Lib/TRUNetworking2'
    pod 'loginwork', :path => 'Lib/loginwork'
  # Pods for componentT

  target 'componentTTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'componentTUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
